# คู่มือการใช้งานและโครงสร้างกลไก Stake Dice Bot (Project 3)

เอกสารฉบับนี้รวบรวมรายละเอียดฟีเจอร์ กลไกทางคณิตศาสตร์ และกฎระเบียบขั้นเด็ดขาดสำหรับการทำงานของบอทและ AI ที่เข้ามาดูแลโปรเจกต์นี้

---

## 🚀 1. ฟีเจอร์และกลไกหลักของบอท (Core Features)

### 1.1. ระบบทวงทุนคืน (The Money Glitch Recovery Formula)
บอทใช้สมการเดินเงินแบบ Martingale ที่ถูกปรับแต่งพิเศษเพื่อเรียกทุนคืนพร้อมทำกำไรแบบตายตัวเสมอ:
- **เป้าหมายกำไร (Target Profit):** `base_bet * 0.5`
- **อัตราจ่าย (Payout Multiplier):** `99.0 / Win Chance`
- **สมการเดิมพันแก้ตัว:** `(ยอดเสียสะสม + เป้าหมายกำไร) / (อัตราจ่าย - 1.0)`
- **ผลลัพธ์:** ทันทีที่แทงชนะ บอทจะดึงทุนที่เสียไปในสตรีคแดงกลับมาทั้งหมด และได้กำไรสุทธิเท่ากับ `base_bet * 0.5` เสมอ

### 1.2. จุดสมดุลความเสี่ยง (The +1% Sweet Spot)
- เมื่อเกิดสตรีคแดง (แพ้ติดต่อกัน) บอทจะ **เพิ่มโอกาสชนะ (Win Chance) ขึ้นทีละ +1% ต่อตา**
- การบวกเพิ่มแค่ +1% ช่วยลดความผันผวนของอัตราจ่าย ทำให้ตัวคูณ (Payout) ไม่ตกต่ำเร็วเกินไป 
- ส่งผลให้ยอดทบเงินไม่กระโดดเป็นหลักล้าน และช่วยรักษาอายุสายป่าน (Safe Losses) ให้ยาวนานที่สุดเมื่อเทียบกับต้นทุน

### 1.3. ระบบสัดส่วนเงินทุนอัตโนมัติ (Auto-Compound / Dynamic Percent)
- เปิด-ปิดได้ผ่านค่า `dynamic_percent` ในไฟล์ `config.json`
- หากตั้งค่าเป็นตัวเลข (เช่น `0.000375`) บอทจะคำนวณไม้แรก (Base Bet) โดยอัตโนมัติจาก **สัดส่วนเงินทุนปัจจุบัน** (`balance * (dynamic_percent / 100)`)
- ทำให้กำไรโตขึ้นเรื่อยๆ ตามขนาดพอร์ตที่ใหญ่ขึ้น โดยที่ระดับความเสี่ยง (Safe Losses) ยังคงถูกล็อกไว้เท่าเดิม

### 1.4. มาตรวัดความปลอดภัย (Safe Losses Indicator)
- บนหน้า Dashboard จะมีการแสดงผล **Safe Losses** แบบ Real-time
- คำนวณจากสมการจำลองเหตุการณ์ล่วงหน้า โดยเช็คว่าด้วย "เงินทุนปัจจุบัน" และ "Base Bet ปัจจุบัน" พอร์ตจะสามารถทนสตรีคแดงได้สูงสุดกี่ตาโดยที่ยังมีเงินเหลือพอให้วางเดิมพันในตาถัดไป
- หากเงินทุนเหลือน้อย บอทจะดรอป Base Bet ลงไปที่ **Min Bet ต่ำสุดของคาสิโน** (`0.00000001 TRX`) โดยอัตโนมัติเพื่อลดความเจ็บปวด

### 1.5. ระบบสลับบัญชีอัจฉริยะ (8-Account Rotation)
- จัดการบอท 8 บัญชี แบ่งเป็น 4 กลุ่ม (A, B, C, D) กลุ่มละ 2 บัญชีรันพร้อมกัน
- ควบคุมผ่าน `switch_controller.py` และ `start_rotation.bat`
- **ระบบพักเมื่อขาดทุน:** กลุ่มที่ยังมียอด Net Profit ติดลบ จะถูกบังคับไม่ให้สลับไปกลุ่มอื่นจนกว่าจะดึงทุนกลับมาเป็นบวก (ป้องกันการสะสมยอดขาดทุนค้างทิ้งไว้)
- **ระบบป้องกันเงินหมด (Insufficient Funds Guard):** หากเช็คเจอว่ายอดเงินไม่พอให้ลงเดิมพันตามแผน บอทจะปิดตัวเองแล้วสลับไปกลุ่มอื่นทันที

### 1.6. ระบบสุ่มกลยุทธ์อัตโนมัติ (Random Strategy Rotation)
เพื่อป้องกันการจับทางได้ บอทจะมีระบบสุ่ม 50/50 สลับใช้ระหว่าง 2 กลยุทธ์หลัก:
1. **Advanced Martingale:** (สูตรหลักดั้งเดิม) เริ่มโอกาสชนะ 66% เมื่อเสียเพิ่ม Win Chance ทีละ 1% พร้อมคำนวณเงินกู้ทุน ทันทีที่ชนะจะดึงทุนคืนและกำไรตามเป้า
2. **Custom 45/40:** โอกาสชนะ 49.50% (จ่าย 2.0x) เมื่อเสียจะทบเงิน 45% หากชนะจะลดเงิน 20% (ขั้นต่ำไม่ต่ำกว่า 0.00032) เมื่อชนะครบ 3 ครั้งติดกันจึงจะรีเซ็ตรอบ
- **เงื่อนไขความปลอดภัย:** บอทจะทำการ "สุ่มเปลี่ยนกลยุทธ์" เฉพาะตอนที่จบรอบเดิมพัน (กู้ทุนสำเร็จและกลับมาที่ Base Bet) เท่านั้น เพื่อไม่ให้สูตรรวนขณะตามทุน

---

## 🚫 2. กฎเหล็กข้อบังคับสำหรับ AI (STRICT AI RULES)

> [!CAUTION]
> **คำสั่งเด็ดขาดจากผู้ใช้:** AI ทุกตัวต้องอ่านกฎนี้และทำตามอย่างเคร่งครัด **ห้ามแก้ไขโค้ดมั่วๆ หรือปรับโครงสร้างไฟล์โดยไม่ได้รับคำสั่งตรงจากผู้ใช้เด็ดขาด**

### 2.1. ห้ามแตะต้องสมการคณิตศาสตร์
- **ห้าม** เปลี่ยนแปลงสูตรเดินเงิน `(sim_accumulated_loss + sim_target_profit) / (sim_payout - 1.0)`
- **ห้าม** เปลี่ยนแปลงกฎการบวก Win Chance (`+1%`) กลับไปเป็นค่าอื่น เว้นแต่ผู้ใช้จะระบุตัวเลขใหม่ชัดเจน
- **ห้าม** แตะต้องฟังก์ชันคำนวณ `calculate_max_safe_losses` หากไม่มีคำสั่งให้เปลี่ยนแปลงตัวแปรโครงสร้าง

### 2.2. ห้ามมโนหรือคาดเดาเจตนา
- หากผู้ใช้สั่งให้แก้บั๊ก หรือแก้ Dashboard **ห้าม** ไปเปลี่ยนพฤติกรรมการเดิมพัน
- หากข้อมูลไม่พอ ให้หยุดและ **"ถามผู้ใช้"** ห้ามคิดสร้างเงื่อนไขใหม่เอง
- ห้ามแก้จากความจำ หรือใช้ชื่อฟังก์ชันเก่าที่ไม่มีอยู่ในโค้ดปัจจุบัน

### 2.3. ความปลอดภัยของข้อมูล
- **ห้ามเปิดเผย** token, cookies, proxy, สเตตัส API หรือข้อมูลส่วนตัวของบัญชีที่อยู่ใน `config.json` หรือ `log`
- **ห้าม** ล้างประวัติ (Reset history), ลบไฟล์ log, หรือแก้ไขยอดโชว์หน้า Dashboard แบบหลอกๆ
- ห้ามดัดแปลง `dice_stats*.json` ด้วยมือระหว่างที่บอทกำลังรันอยู่

### 2.4. ขั้นตอนการส่งงาน
1. ต้องตรวจเช็คไฟล์ปัจจุบันก่อนแก้ไขเสมอ (อย่าสมมติเอาเอง)
2. แก้ไขแบบเจาะจงเป็นจุด (Targeted fix) ไม่ทำ Regex กวาดทั้งไฟล์
3. ต้องรายงานให้ผู้ใช้ทราบเสมอว่าแก้ไฟล์ไหน และผลลัพธ์ที่เปลี่ยนไปคืออะไร
4. ห้ามรัน Live Bet เพื่อทดสอบ ให้ใช้ Simulation หรือ Calculator แทนเท่านั้น
